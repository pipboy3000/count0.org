---
layout: null
---
{
  "categories": {
    {% for cat in site.categories %}
      "{{ cat[0] }}": {
        "posts": {{ cat[1].size }},
        "url": "/categories/{{ cat[0] }}/"
      }{% unless forloop.last %},{% endunless %}
    {% endfor %}
    }
}
